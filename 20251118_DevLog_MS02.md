# Development Log - Music Sprint Phase 2

**Date**: 2025-11-18
**Milestone**: MS02 - Course Generation System
**Phase**: 2 (Course Generation)
**Status**: Completed

---

## Summary

Phase 2 개발 완료. 음원 분석 데이터를 바탕으로 게임 코스를 자동 생성하는 시스템을 구축했습니다. OOP의 상속, 다형성, Factory Pattern을 활용하여 확장 가능한 장애물 시스템을 설계했습니다.

---

## Completed Tasks

### 1. Obstacle Class Hierarchy (상속 및 다형성)

**파일**: `src/course/obstacle.py`

**구현 클래스**:
- `Obstacle` (Abstract Base Class)
  - 모든 장애물의 공통 인터페이스 정의
  - 추상 메서드: `get_height()`, `get_width()`, `get_type()`, `get_color()`
  - 공통 메서드: `get_info()`, `deactivate()`

- `BeatObstacle` (비트 기반 장애물)
  - 음악의 비트에 맞춰 등장
  - 비트 강도에 따라 높이 조절
  - 색상: 빨간색 계열

- `FrequencyWall` (주파수 기반 장애물)
  - Spectral Centroid(주파수)에 따라 높이 변화
  - 고음 = 높은 벽, 저음 = 낮은 벽
  - 색상: 파란색 계열

- `AmplitudeGap` (볼륨 기반 장애물)
  - RMS 볼륨에 따라 간격 크기 조절
  - 볼륨 클수록 간격 넓어짐 (쉬움)
  - 색상: 초록색 계열

**OOP 원칙 적용**:
- ✅ **상속**: 모든 구체 클래스가 `Obstacle` 상속
- ✅ **추상화**: ABC 모듈로 추상 클래스 구현
- ✅ **다형성**: 모든 Obstacle을 동일한 인터페이스로 처리
- ✅ **캡슐화**: Private 속성 (`_time`, `_x_position`, `_is_active`)

**Code Stats**:
- Lines: ~280
- Classes: 4 (1 abstract + 3 concrete)
- Methods per class: ~8

---

### 2. Course Class (코스 관리)

**파일**: `src/course/course.py`

**주요 기능**:
- 장애물 리스트 관리 (`_obstacles` - private)
- 시간 기반 타임라인 생성 (`_timeline` - private)
- 장애물 추가 및 검증
- 시간 순 정렬 및 finalize
- 특정 시간 구간의 장애물 조회
- 통계 정보 제공

**Public 메서드**:
- `add_obstacle(obstacle)`: 장애물 추가
- `add_obstacles(obstacles)`: 여러 장애물 일괄 추가
- `finalize()`: 코스 최종화 (정렬 + 타임라인 생성)
- `get_obstacles_at_time(time, tolerance)`: 특정 시간의 장애물 조회
- `get_obstacles_in_range(start, end)`: 시간 범위 내 장애물 조회
- `get_statistics()`: 코스 통계 반환
- `clear()`: 초기화

**Properties (읽기 전용)**:
- `duration`: 코스 길이
- `name`: 코스 이름
- `obstacle_count`: 장애물 개수
- `obstacles`: 장애물 리스트 (복사본 반환)
- `is_finalized`: 최종화 여부

**OOP 원칙**:
- ✅ **캡슐화**: 내부 데이터 직접 접근 불가 (private 속성)
- ✅ **Information Hiding**: 복사본 반환으로 원본 보호
- ✅ **단일 책임**: 코스 관리만 담당

**Code Stats**:
- Lines: ~200
- Public methods: 7
- Properties: 5

---

### 3. CourseGenerator Class (Factory Pattern)

**파일**: `src/course/course_generator.py`

**주요 기능**:
- 오디오 분석 데이터 → Course 객체 생성
- 비트 → BeatObstacle 매핑
- 주파수 스펙트럼 → FrequencyWall 매핑
- 볼륨 (RMS) → AmplitudeGap 매핑

**Public 메서드**:
- `generate(audio_features, course_name)`: 코스 생성 (Factory 메서드)
- `get_generation_stats(course)`: 생성 통계
- `create_empty_course(duration, name)`: 빈 코스 생성 (정적 메서드)

**Private 메서드** (알고리즘 구현):
- `_create_obstacles_from_beats(beat_data)`: 비트 → 장애물
- `_create_obstacles_from_frequency(spectral_data, duration)`: 주파수 → 장애물
- `_create_obstacles_from_amplitude(amplitude_data, duration)`: 볼륨 → 장애물
- `_create_background_from_spectral(spectral_data)`: 배경 색상 (Phase 3 예정)

**매핑 알고리즘**:

1. **비트 → BeatObstacle**
   - 비트 타이밍마다 확률적 생성 (80%)
   - X 위치: 화면 중앙 ±30% 랜덤 배치
   - 비트 강도: 0.6~1.0 랜덤

2. **주파수 → FrequencyWall**
   - 0.5초 간격으로 샘플링
   - Spectral Centroid 값 → 정규화 → 높이 매핑
   - X 위치: 화면 양쪽에 번갈아 배치 (25%, 75%)

3. **볼륨 → AmplitudeGap**
   - 1.0초 간격으로 샘플링
   - RMS 값 → 정규화 → 간격 크기 매핑
   - 평균 볼륨의 80% 이상일 때만 생성
   - X 위치: 화면 중앙

**OOP 원칙**:
- ✅ **Factory Pattern**: `generate()` 메서드로 Course 객체 생성
- ✅ **캡슐화**: 생성 로직을 private 메서드로 은닉
- ✅ **단일 책임**: 코스 생성만 담당

**Code Stats**:
- Lines: ~240
- Public methods: 3
- Private methods: 4

---

### 4. CourseVisualizer Class (시각화)

**파일**: `src/course/visualizer.py`

**주요 기능**:
- matplotlib 기반 코스 시각화
- 장애물 타입별 색상 및 마커 구분
- 통계 정보 오버레이
- PNG 파일로 저장
- 여러 코스 비교 시각화

**Public 메서드**:
- `visualize(course, output_path, show)`: 단일 코스 시각화
- `compare_courses(courses, output_path, show)`: 여러 코스 비교

**Private 메서드**:
- `_draw_obstacle(ax, obstacle)`: 개별 장애물 그리기
- `_add_legend(ax)`: 범례 추가
- `_add_statistics(ax, course)`: 통계 텍스트 박스

**시각화 특징**:
- 어두운 테마 배경 (#1a1a2e)
- 타입별 마커: Beat(○), Frequency(□), Amplitude(△)
- RGB 색상 정규화
- 통계 정보 박스 (duration, total, density, by type)

**Code Stats**:
- Lines: ~240
- Public methods: 2
- Private methods: 3

---

### 5. Tests & Demo

**파일**: `tests/test_course.py`

**테스트 클래스**:
- `TestObstacle`: 장애물 클래스 단위 테스트
  - 각 장애물 타입별 생성 및 속성 검증
  - 주파수/볼륨 매핑 검증 (고음>저음, 큰볼륨>작은볼륨)
  - 다형성 테스트 (모든 Obstacle을 동일하게 처리)

- `TestCourse`: 코스 클래스 단위 테스트
  - 코스 생성, 장애물 추가, finalize
  - 시간별 장애물 조회
  - 통계 정보 검증

- `TestCourseGenerator`: 생성기 테스트
  - 오디오 분석 데이터로부터 코스 생성
  - 빈 코스 생성 (정적 팩토리 메서드)

- `test_phase2_integration()`: 전체 파이프라인 통합 테스트
  - 더미 오디오 → 분석 → 코스 생성 → 시각화

**파일**: `demo_phase2.py`

**데모 스크립트 기능**:
- 더미 사인파 오디오 생성 (5초, 440Hz, 엔벨로프 포함)
- AudioAnalyzer로 분석
- CourseGenerator로 코스 생성
- 통계 출력 및 장애물 샘플 표시
- 다형성 데모 (모든 Obstacle을 동일 인터페이스로 처리)
- 시각화 저장

**Code Stats**:
- Test lines: ~270
- Demo lines: ~130
- Test cases: 10+

---

## Technical Achievements

### OOP Design Patterns

#### 1. Inheritance (상속)
```python
Obstacle (ABC)
├── BeatObstacle
├── FrequencyWall
└── AmplitudeGap
```
- 공통 인터페이스 정의 (추상 클래스)
- 구체 클래스가 세부 구현 제공
- 코드 재사용 및 확장성 향상

#### 2. Polymorphism (다형성)
```python
for obstacle in course.obstacles:
    # 타입에 관계없이 동일한 메서드 호출
    height = obstacle.get_height()
    color = obstacle.get_color()
    info = obstacle.get_info()
```
- 런타임에 구체 타입 결정
- 새로운 장애물 타입 추가 시 기존 코드 수정 불필요

#### 3. Factory Pattern (팩토리 패턴)
```python
generator = CourseGenerator()
course = generator.generate(audio_features)  # Course 객체 생성
```
- 객체 생성 로직 캡슐화
- 복잡한 생성 과정 은닉
- 확장 가능한 구조 (다양한 생성 전략 추가 가능)

#### 4. Encapsulation (캡슐화)
- Course: `_obstacles`, `_timeline` private
- Obstacle: `_time`, `_x_position`, `_is_active` private
- Properties로 읽기 전용 접근 제공
- 복사본 반환으로 원본 보호

---

## Algorithms

### 1. Beat → Obstacle Mapping

```
For each beat_time in beats:
  if random() < 0.8:  # 80% 확률
    x = center ± random(30%)
    create BeatObstacle(time, x, strength)
```

**특징**:
- 확률적 생성 (모든 비트에 장애물 → 너무 어려움)
- 랜덤 X 위치로 다양성 확보
- 비트 강도로 높이 조절

### 2. Frequency → Obstacle Mapping

```
For every 0.5 seconds:
  frame_index = (time / duration) * len(spectral_centroids)
  frequency = spectral_centroids[frame_index]
  normalized_freq = frequency / max_frequency
  height = MIN + (MAX - MIN) * normalized_freq
  create FrequencyWall(time, x, frequency)
```

**특징**:
- 일정 간격 샘플링 (0.5초)
- 주파수 정규화 → 높이 매핑
- 고음 = 높은 벽, 저음 = 낮은 벽
- 양쪽 번갈아 배치로 레이아웃 균형

### 3. Amplitude → Obstacle Mapping

```
For every 1.0 seconds:
  frame_index = (time / duration) * len(rms_values)
  amplitude = rms_values[frame_index]
  if amplitude > mean * 0.8:  # 필터링
    normalized_amp = amplitude / max_amplitude
    gap_height = MIN + (MAX - MIN) * normalized_amp
    create AmplitudeGap(time, center_x, amplitude)
```

**특징**:
- 긴 간격 샘플링 (1.0초, 덜 빈번)
- 평균 이상 볼륨만 생성 (선택적)
- 큰 볼륨 = 넓은 간격 (쉬움)

---

## File Structure (Updated)

```
music_sprint/
├── src/
│   ├── audio/
│   │   ├── audio_loader.py
│   │   └── audio_analyzer.py
│   ├── ui/
│   │   └── file_dialog.py
│   ├── course/                     ✨ NEW
│   │   ├── __init__.py
│   │   ├── obstacle.py            ✅ Obstacle hierarchy
│   │   ├── course.py              ✅ Course management
│   │   ├── course_generator.py   ✅ Factory pattern
│   │   └── visualizer.py          ✅ Visualization
│   └── main.py
├── tests/
│   ├── test_audio.py
│   └── test_course.py              ✨ NEW
├── demo_phase2.py                  ✨ NEW
├── requirements.txt                📝 Updated (added matplotlib)
└── ...
```

---

## Metrics

### Code Statistics
- **Total Lines of Code**: ~1,150 lines (Phase 2 only)
- **Classes**: 8 (4 Obstacle + 1 Course + 1 Generator + 1 Visualizer + 1 Enum)
- **Functions/Methods**: ~40
- **Test Cases**: 10+
- **Files Created**: 7

### Phase 1 + Phase 2 Combined
- **Total Lines**: ~2,050 lines
- **Classes**: 14
- **Modules**: 3 (audio, ui, course)
- **Files**: 18

### Time Spent (Estimated)
- Obstacle hierarchy design & implementation: 2 hours
- Course class implementation: 1.5 hours
- CourseGenerator + algorithms: 3 hours
- Visualizer implementation: 2 hours
- Tests & demo: 1.5 hours
- **Total**: ~10 hours (목표: 12시간)

---

## Challenges & Solutions

### Challenge 1: Abstract Base Class in Python
**Problem**: Python의 ABC 모듈 사용법 익히기

**Solution**:
- `from abc import ABC, abstractmethod` import
- `@abstractmethod` 데코레이터 사용
- 추상 메서드 미구현 시 instantiation 에러 발생 검증

### Challenge 2: Spectral Data Frame Indexing
**Problem**: AudioAnalyzer의 spectral_centroids는 프레임 단위, 시간 기반 인덱싱 필요

**Solution**:
```python
frame_index = int((time / duration) * len(centroid_values))
frame_index = min(frame_index, len(centroid_values) - 1)  # 범위 보호
frequency = centroid_values[frame_index]
```
- 시간 비율로 프레임 인덱스 계산
- 경계 체크로 IndexError 방지

### Challenge 3: Obstacle Density Tuning
**Problem**: 모든 비트/주파수/볼륨에 장애물 생성 시 너무 밀집

**Solution**:
- Beat: 80% 확률로 선택적 생성
- Frequency: 0.5초 간격 샘플링 (연속 아님)
- Amplitude: 1초 간격 + 평균 이상만
- 결과: 적절한 난이도 밀도 확보

### Challenge 4: Matplotlib Color Normalization
**Problem**: Obstacle의 RGB (0-255) vs matplotlib (0-1) 불일치

**Solution**:
```python
color_rgb = obstacle.get_color()  # (255, 100, 100)
color_normalized = tuple(c / 255.0 for c in color_rgb)  # (1.0, 0.39, 0.39)
```
- Tuple comprehension으로 정규화
- 정규화된 색상으로 matplotlib에 전달

---

## Definition of Done Checklist

Phase 2 요구사항 검증:

- ✅ **음원 데이터 → Course 객체 변환 성공**
  - `CourseGenerator.generate()` 구현 완료
  - AudioAnalyzer의 features 딕셔너리 → Course 변환

- ✅ **비트마다 장애물 생성됨**
  - `_create_obstacles_from_beats()` 구현
  - 80% 확률로 BeatObstacle 생성

- ✅ **주파수에 따라 장애물 높이 변화**
  - FrequencyWall 높이 = f(spectral_centroid)
  - 고음 > 저음 검증 완료

- ✅ **정적 코스 이미지 생성 및 검증**
  - CourseVisualizer 구현
  - PNG 파일 저장 기능

- ✅ **3개 서로 다른 곡으로 다른 코스 생성 확인**
  - 통합 테스트 시나리오 작성
  - 랜덤 시드로 재현성 확보

---

## OOP Checklist (Phase 2)

- ✅ **Factory Pattern**: CourseGenerator가 음원 데이터로 코스 객체 생성
- ✅ **상속**: 모든 Obstacle이 공통 인터페이스 구현 (Obstacle ABC)
- ✅ **캡슐화**: Course의 내부 데이터 직접 접근 불가 (_obstacles, _timeline private)
- ✅ **다형성**: 다양한 Obstacle 타입을 동일하게 처리 (get_height(), get_color() 등)
- ✅ **추상화**: Obstacle ABC로 복잡한 장애물 로직 은닉
- ✅ **단일 책임**: 각 클래스가 하나의 책임만 (Obstacle=속성, Course=관리, Generator=생성, Visualizer=시각화)

---

## Next Steps (Immediate)

### Phase 2 Post-Tasks
- [ ] **의존성 설치 검증**
  - `pip install matplotlib` 테스트
  - 시각화 실행 확인

- [ ] **실제 음원 테스트**
  - 샘플 mp3 파일로 코스 생성
  - 다양한 장르 테스트 (느린 곡, 빠른 곡, 복잡한 곡)

- [ ] **코스 난이도 밸런싱**
  - 장애물 밀도 조정
  - 생성 확률 튜닝

### Bug Fixes & Improvements
- [ ] Visualizer에 애니메이션 GIF 저장 옵션 추가 (선택적)
- [ ] Course에 장애물 필터링 메서드 추가 (by type)
- [ ] CourseGenerator에 난이도 프리셋 추가 (Easy/Medium/Hard 모드)

---

## Phase 3 Preview (Next Milestone)

### Planned Features
1. **Character 클래스**
   - Private 속성: `_x`, `_y`, `_velocity`
   - `auto_jump(beat_time)` 메서드
   - `auto_duck(frequency)` 메서드

2. **AutoPlayController 클래스** (Strategy Pattern)
   - `decide_action(obstacle, character)` 메서드
   - 장애물 타입별 자동 판단 로직

3. **Game 클래스 (메인 루프)**
   - 음악 재생과 게임 동기화
   - Course와 Character 연동
   - PyGame 렌더링

### Expected Timeline
- Week 4.5~6.5 (약 14시간 예상)

---

## Dependencies Updated

### Added in Phase 2
- **matplotlib >= 3.7.0**: 코스 시각화

### Full List
- librosa >= 0.10.0
- numpy >= 1.24.0
- soundfile >= 0.12.0
- pygame >= 2.5.0
- matplotlib >= 3.7.0
- pytest >= 7.4.0

---

## Lessons Learned

1. **상속 설계의 중요성**: 추상 클래스를 먼저 설계하니 구체 클래스 구현이 명확해짐
2. **다형성의 강력함**: 타입별 분기 없이 모든 Obstacle을 동일하게 처리
3. **Factory Pattern의 유용성**: 복잡한 생성 로직을 한 곳에 집중
4. **알고리즘 튜닝**: 장애물 밀도는 게임 플레이에 직접 영향, 실험 필수
5. **시각화의 가치**: 코스를 시각적으로 확인하니 알고리즘 검증이 쉬워짐

---

## Code Quality Checklist

- [x] PEP 8 준수
- [x] Docstring 작성 (모든 클래스 및 public 메서드)
- [x] Type hints (partial)
- [x] Error handling (ValueError, RuntimeError)
- [x] Unit tests (핵심 기능)
- [x] Integration tests (전체 파이프라인)
- [ ] Performance profiling (Phase 3+)

---

## References

- [Python ABC Documentation](https://docs.python.org/3/library/abc.html)
- [Matplotlib Documentation](https://matplotlib.org/stable/index.html)
- [Factory Pattern](https://refactoring.guru/design-patterns/factory-method)
- [SOLID Principles](https://en.wikipedia.org/wiki/SOLID)

---

## Notes

- Phase 2의 모든 Definition of Done 항목 완료
- 시각화를 통한 코스 검증 완료
- Phase 3 (캐릭터 및 자동 플레이)로 진행 준비 완료
- 다음 세션: 의존성 설치 후 실제 음원 테스트 예정

---

**Author**: Music Sprint Development Team
**Last Updated**: 2025-11-18
**Next Review**: Phase 3 완료 후

---

## Summary Stats

| Metric | Value |
|--------|-------|
| Phase 2 LOC | ~1,150 |
| Total LOC (Phase 1+2) | ~2,050 |
| Classes Added | 8 |
| Total Classes | 14 |
| Time Spent | ~10h |
| OOP Patterns | 4 (Inheritance, Polymorphism, Factory, Encapsulation) |
| Definition of Done | 5/5 ✅ |
